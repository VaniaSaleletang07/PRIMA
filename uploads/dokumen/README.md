# Document Upload Directory

This directory stores documents uploaded by pengurus (tank truck managers).

Files are stored with random filenames for security.
Original filenames are stored in the `dokumen_kendaraan` database table.
